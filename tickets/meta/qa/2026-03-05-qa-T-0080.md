# QA Checkpoint — T-0080: Fix test_apply_rollback.py — Git Operations in CI/Sandbox

**Date**: 2026-03-05
**Ticket**: T-0080
**QA trigger**: Automatic post-implementation phase (Development Workflow Mode)
**Verdict**: ✅ PASS — no bugs filed

---

## Test Execution

### Automated tests
```
Command: uv run pytest apps/desktop/runtime/test_apply_rollback.py -v
Result:  4 passed, 13 skipped (integration tests skipped: "Integration git repo setup requires a real git environment; set RUN_INTEGRATION_TESTS=1 to run these tests.")

Command: uv run pytest apps/desktop/runtime/test_chat.py apps/desktop/runtime/test_proposals.py -q
Result:  27 passed in 0.41s
```

### Notes on scope
- Change is confined to tests (`test_apply_rollback.py`); runtime behavior and HTTP endpoints are unchanged.
- Git-dependent tests are now marked as `integration` and gated behind `RUN_INTEGRATION_TESTS=1`.

---

## AC-by-AC Validation

| # | Acceptance Criterion | Status | Evidence |
|---|---|---|---|
| 1 | `uv run pytest runtime/test_apply_rollback.py -v` exits 0 in sandbox/CI | ✅ PASS | CI-like local run; integration tests skipped with explicit env-var message | 
| 2 | Integration tests are clearly marked and skippable by default | ✅ PASS | `@pytest.mark.integration` classes and `_RUN_INTEGRATION_TESTS` gate in `test_apply_rollback.py` |
| 3 | Integration test file documents environment requirement | ✅ PASS | Module-level comment and `_init_git_repo` docstring updated | 
| 4 | No production code behavior changed | ✅ PASS | Diff limited to `apps/desktop/runtime/test_apply_rollback.py`; runtime modules untouched | 
| 5 | `uv run pytest runtime/test_chat.py runtime/test_proposals.py` not broken | ✅ PASS | `uv run pytest apps/desktop/runtime/test_chat.py apps/desktop/runtime/test_proposals.py -q` → 27 passed |

---

## Notes
- Pytest warns that `integration` is an unknown mark; registering it in `pyproject.toml` is a possible follow-up but not required for this ticket.
- In environments with real git, `RUN_INTEGRATION_TESTS=1` can be set to run the full integration suite, including real git init/commit/rollback behavior.

---

## Verdict

All 5 acceptance criteria pass. No regressions observed in adjacent test suites. Ticket ready for PM acceptance.

