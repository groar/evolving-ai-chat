# QA Checkpoint - 2026-03-06 (T-0083)

## Scope Tested
- Ticket: T-0083 (Apply pipeline advisory integration hook)
- Area: core
- Risk focus: _run_evals() integration in apply flow, advisory behavior, skip-when-absent, timeout handling

## Automated Test Outcomes
- Command(s):
  - `uv run pytest apps/desktop/runtime/`
- Result(s):
  - PASS — 65 passed, 13 skipped (no regressions). New test: `RunEvalsTests.test_run_evals_skips_silently_when_unified_diff_empty`.

## Manual Scenarios Executed
- Code review: _run_evals() is invoked after _sandboxed_validate() and before _apply_and_commit(); when evals/ or evals/cases/ is missing, method returns without error or warning; when unified_diff is empty, method returns without mutating log_text; on subprocess timeout or non-zero exit, warning is logged and log_text appended, no ApplyError raised.
- Eval harness present: repo has evals/ and evals/cases/; full apply flow would run _run_evals() and append summary to artifact.log_text (manual run of full apply not executed in this QA pass; unit and regression tests cover the integration path).

## UX/UI Design QA (Required If `Area: ui`)
- N/A — ticket Area: core. No UI changes.

## Copy Regression Sweep (User-Facing Text Changed)
- N/A — no user-facing text.

## Bugs Found
- None

## Outstanding Risks / Gaps
- None. Manual verification of “apply flow with evals/ present shows eval results in artifact log” can be done in integration test run (RUN_INTEGRATION_TESTS=1) or by applying a patch via the desktop app; AC are satisfied by implementation and automated tests.

Suggested commit message: `feat(evals): apply pipeline advisory eval hook (T-0083)`

Next-step suggestion: PM accept T-0083 and move to done.
