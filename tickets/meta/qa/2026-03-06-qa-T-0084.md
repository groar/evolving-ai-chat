# QA Checkpoint - 2026-03-06 (T-0084)

## Scope Tested
- Ticket: T-0084 (Eval harness test coverage + STATUS.md cleanup)
- Area: core
- Risk focus: test_evals.py subprocess correctness, JSON structure assertions, full suite regression, STATUS.md known-gap accuracy

## Automated Test Outcomes
- Command(s):
  - `uv run pytest apps/desktop/runtime/test_evals.py`
  - `uv run pytest` (full suite)
- Result(s):
  - PASS — test_evals.py: 6 passed (good case exit 0 + JSON, bad case expectation-matched + JSON, cases-dir both results).
  - PASS — full suite: 71 passed, 13 skipped (no regressions).

## Manual Scenarios Executed
- Good case: `uv run python apps/desktop/runtime/evals/run.py --case apps/desktop/runtime/evals/cases/good_patch_applies.yaml` → exit 0, JSON total=1, passed=1, results[0].message "Patch applies cleanly (dry-run)".
- Bad case: same with bad_patch_applies.yaml → exit 0 (expectation matched), JSON total=1, passed=1, results[0].expected "fail", message shows patch failed to apply as expected.
- STATUS.md: "Known gaps" — "No eval harness" replaced with strikethrough and M12 completion note; no other unintended edits.

## UX/UI Design QA (Required If `Area: ui`)
- N/A — ticket Area: core. No UI changes.

## Copy Regression Sweep (User-Facing Text Changed)
- N/A — no user-facing text (STATUS.md is internal/product doc).

## Bugs Found
- None

## Outstanding Risks / Gaps
- None.

Suggested commit message: `test(evals): add test_evals.py + STATUS.md known-gap cleanup (T-0084)`

Next-step suggestion: PM accept T-0084 and move to done.
