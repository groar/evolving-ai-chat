# QA Checkpoint: T-0095 (Fix with AI — git commit when agent succeeds)

**Date**: 2026-03-08  
**Ticket**: T-0095  
**Scope**: Apply pipeline timeout defaults and env configurability; validation_timeout handling and UI copy.

## Test Plan
- Automated: runtime pytest (apply_pipeline, apply/rollback, retry, patch_agent).
- Manual: Confirm timeout constants and env override behavior; confirm no regression for scope_blocked/validation_failed.
- Copy: New `validation_timeout` message in PatchNotification.

## Results

### Automated
- `uv run pytest apps/desktop/runtime/`: **109 passed**, 13 skipped. No failures.
- `test_patch_timeout_raises_apply_error_patch_timeout` passes with current `_PATCH_TIMEOUT` in details.

### Manual / Code Review
- Default timeouts: 300s for both patch and validate (was 180s / 120s). Env `APPLY_PATCH_TIMEOUT_SEC` and `APPLY_VALIDATE_TIMEOUT_SEC` read at module load; min 60s enforced.
- `_sandboxed_validate`: `TimeoutExpired` for npm run validate now raises `ApplyError("validation_timeout", ...)` (no longer falls through to "unexpected_error").
- Scope_blocked and validation_failed paths unchanged; only timeout values and new failure reason added.

### Copy
- `getFailureReasonCopy("validation_timeout")`: "validation timed out (build/lint took too long). You can increase APPLY_VALIDATE_TIMEOUT_SEC and try again." — Clear, actionable; term consistency with patch_timeout message.

### UX/UI
- Area: core. No UX/UI Design QA checklist required. Visual smoke: error message would appear in PatchNotification banner if validation_timeout occurred; not exercised in this run.

## Findings
- **PASS**. Acceptance criteria met; no bugs found. Ready for PM acceptance.

## Suggested Commit Message
```
fix(apply-pipeline): configurable timeouts + 300s defaults so Fix with AI commits (T-0095)
```

## Next Step
PM acceptance: move T-0095 to `done/` and update ORDER.md.
