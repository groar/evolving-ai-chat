# QA Checkpoint - 2026-03-06 (T-0082)

## Scope Tested
- Ticket: T-0082 (Eval harness core + first check `patch_applies_cleanly`)
- Area: core
- Risk focus: new eval harness entry point, YAML case loader, patch_applies_cleanly check, CLI contract

## Automated Test Outcomes
- Command(s):
  - `uv run pytest` (project root)
  - `uv run python apps/desktop/runtime/evals/run.py` (default cases)
- Result(s):
  - PASS — 64 passed, 13 skipped (no regressions)
  - Eval harness exits 0; JSON stdout shows total: 2, passed: 2; good-patch-applies (expect pass) and bad-patch-applies (expect fail) both matched.

## Manual Scenarios Executed
- Default run: `uv run python apps/desktop/runtime/evals/run.py` → exit 0, both cases match expectations.
- Single case: `--case apps/desktop/runtime/evals/cases/good_patch_applies.yaml` → exit 0, 1 result passed.
- Verbose: `--verbose` → exit 0, JSON unchanged.
- Empty cases dir: `--case /tmp/empty-evals-cases` → exit 0, `{"total": 0, ...}`, stderr "No eval cases found, skipping."

## UX/UI Design QA (Required If `Area: ui`)
- N/A — ticket Area: core. No UI changes.

## Copy Regression Sweep (User-Facing Text Changed)
- N/A — no user-facing text.

## Bugs Found
- None

## Outstanding Risks / Gaps
- None. T-0084 will add dedicated test_evals.py; this ticket’s AC are satisfied.

Suggested commit message: `feat(evals): eval harness core + patch_applies_cleanly check (T-0082)`

Next-step suggestion: PM accept T-0082 and move to done.
