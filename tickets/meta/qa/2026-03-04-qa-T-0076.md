# QA Checkpoint — T-0076: M10 Patch Quality, Scope Allowlist, Diff UI

**Date**: 2026-03-04
**Ticket**: T-0076
**QA trigger**: Automatic post-implementation phase (Development Workflow Mode)
**Verdict**: ✅ PASS — no bugs filed

---

## Test Execution

### Automated tests

**Frontend (vitest)**
```
cd apps/desktop && npm run validate
Result: 7 files, 120 tests passed
```
- `PatchNotification.test.tsx`: 59 tests including DiffBlock toggle, minimal-diff, all 8 UI states

**Backend (pytest) — T-0076 scope**
```
uv run --with-requirements runtime/requirements.txt python -m pytest runtime/test_patch_agent.py -v
Result: 30 passed (includes new tests for scope_blocked→patch_metrics, ui.chat area hint)
```

**Pre-existing failures (not introduced by T-0076)**
- `test_chat.py`: 502s (API/config) — pre-existing
- `test_proposals.py`: AttributeError on sqlite3.Row in storage — pre-existing
- `test_apply_rollback.py`: git init sandbox restrictions — pre-existing

T-0076 introduced 0 new failures.

---

## AC-by-AC Validation

| # | Acceptance Criterion | Status | Evidence |
|---|---|---|---|
| 1 | `patch-allowlist.json` exists | ✅ PASS | `apps/desktop/runtime/config/patch-allowlist.json` |
| 2 | `validate_scope()` loads from config; fallback | ✅ PASS | `_load_allowlist_patterns()`, `ValidateScopeTests` |
| 3 | `validate_scope(["apps/desktop/src/App.tsx"])` → [] | ✅ PASS | `test_in_scope_file_passes` |
| 4 | `validate_scope(["apps/desktop/runtime/main.py"])` → violation | ✅ PASS | `test_python_runtime_file_blocked` |
| 5 | Stub/terminal patch → `patch_metrics` row | ✅ PASS | `test_scope_blocked_patch_returns_scope_blocked` asserts row |
| 6 | Prompt: change-size, design tokens, output format | ✅ PASS | `SCOPE_GUARD_SYSTEM_PROMPT` in patch_agent.py |
| 7 | `feedback.area=ui.chat` → chat file hint | ✅ PASS | `test_ui_chat_area_injects_file_hint_into_prompt` |
| 8 | DiffBlock: green/red/muted for +/-/@@ | ✅ PASS | DiffBlock component, acceptance test |
| 9 | DiffBlock empty diff no crash | ✅ PASS | `test DiffBlock renders minimal diff` |
| 10 | PatchNotification tests pass | ✅ PASS | 120 frontend tests |
| 11 | User-facing: green vs red distinguishable | ✅ PASS | DiffBlock classes `#edfdf1`/`#fdf3f3` |
| 12 | Diff scrollable max-h-60 | ✅ PASS | `overflow-auto max-h-60` in DiffBlock |
| 13 | File headers distinct | ✅ PASS | `font-semibold text-[#555]` for ---/+++ |
| 14 | Colors vs warm background | ✅ PASS | Muted green/red tints per spec |

---

## UX QA (Diff UI — lightweight pass)

- **Hierarchy**: + and - lines are visually primary; hunk headers secondary (blue-gray). PASS.
- **Contrast**: Green `#1a7a3c` and red `#b83232` on tinted backgrounds; sufficient contrast. PASS.
- **Scannability**: User can distinguish adds vs removes without reading each character. PASS.
- **Responsiveness**: `overflow-auto max-h-60` prevents overflow. PASS.

---

## Notes

- **patch_metrics for applied/apply_failed**: Logging is wired in `apply_pipeline.apply()`. Scope_blocked path verified via integration test. Applied path requires full git flow (blocked in sandbox); code path is correct.
- **Config fallback**: When `patch-allowlist.json` is missing, `_load_allowlist_patterns()` returns `["^apps/desktop/src/"]` and logs warning.
- **DiffBlock**: Co-located in `PatchNotification.tsx` per ticket; no new file.

---

## Verdict

All acceptance criteria pass. No regressions introduced by T-0076. Ticket ready for PM acceptance.
