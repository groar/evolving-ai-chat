# QA Checkpoint: T-0097 (Fix with AI — progress in refinement + Activity in-progress cards)

**Date:** 2026-03-08  
**Ticket:** T-0097  
**Scope:** Single-ticket validation after implementation moved to `review/`.

## Test Plan (Scoped)

- **Impacted areas:** Refinement conversation UI, Activity sheet (in-progress + terminal patch cards), patch store and API (feedback_id, refinement_conversation_id), Run agent flow.
- **Automated:** Runtime pytest (patch_agent, refine_context, main); desktop Vite build.
- **Manual/UX:** Progress in refinement view; Activity in-progress cards with "View discussion"; terminal cards with "Discussion"; copy and hierarchy.

## Automated Results

| Check | Result | Notes |
|-------|--------|--------|
| `uv run pytest apps/desktop/runtime` | Pass | 56 passed (incl. PatchArtifact serialization roundtrip). |
| `npm run build` (apps/desktop) | Pass | Vite build succeeded. |
| `npm run validate` | Partial | Desktop build passed; runtime smoke skipped in env (stub/port). |

## UX/UI Checklist (Area: ui) — Heuristic Pass

- **1) Mental Model and Framing:** PASS. Progress in refinement answers "what’s happening" (Working on your change… / Applying… / Retrying…). Activity "View discussion" clarifies that in-progress evolutions link to the refinement view.
- **2) Hierarchy and Focus:** PASS. Primary action in refinement remains Run agent; progress block is secondary status. Activity card primary is title/status; "View discussion" is secondary.
- **3) Information Architecture:** PASS. Single place for progress (refinement view when that feedback’s patch is in flight). Activity exposes in-progress in existing "In progress" details; no duplicate control for same concept.
- **4) States and Error Handling:** PASS. Progress states mirror PatchNotification (pending, applying, retrying + elapsed). No new error surfaces; existing failure handling unchanged.
- **5) Copy and Terminology:** PASS. "View discussion" / "Discussion" are intent-focused. "Working on your change…", "Applying change…", "Retrying…" match existing notification copy; no promise creep.
- **6) Affordances:** PASS. "View discussion" and "Discussion" are explicit buttons; closing Activity sheet on open preserves focus.
- **7) Visual Accessibility:** PASS. Progress block uses same border/background pattern as existing refinement UI; spinner and text contrast retained.
- **8) Responsive:** Not re-tested; no layout changes to rail or sheet width.

**Overall UX checklist:** PASS. No bug or follow-up tickets required for this run.

## Acceptance Criteria → Evidence

| Criterion | Evidence |
|-----------|----------|
| Progress visible in refinement view after Run agent | RefinementConversation receives `activePatch` (in-flight patch for same feedback_id); progress block with elapsed shown; refinement not cancelled on Run agent. |
| In-progress evolutions in Activity as cards with link to discussion | activitySheet shows in-progress in existing "In progress" section; "View discussion" on card (header when collapsed); `onOpenRefinement(feedbackId, title)` opens refinement and closes sheet. |
| Terminal cards optionally retain discussion link | "Discussion" button in expanded section for patches with `feedback_id` (applied/reverted/error). |
| Design spec / no ambiguity | Ticket Design Spec present; implementation matches (progress in refinement, Activity link by feedback_id). |
| pytest / validate | pytest 56 passed; desktop build passed. |

## Copy Regression (User-Facing Text)

- New/updated strings: "View discussion", "Discussion", "Working on your change…", "Applying change…", "Retrying…" (in refinement progress block), "Open refinement discussion to see progress" (aria-label).
- Checked: Consistent with existing PatchNotification wording; no typos; no implied unsupported behavior.

## Findings

- No blocking defects. Implementation aligns with ticket acceptance criteria and design spec.
- **Note:** Patches created before this change will not have `feedback_id` / `refinement_conversation_id`; "View discussion" / "Discussion" only show when `patch.feedback_id` is set (new runs from Fix with AI refinement). Acceptable.

## Bug Tickets

None created.

## Suggested Commit Message

```
feat(T-0097): progress in refinement view + Activity in-progress cards

- Keep refinement open on Run agent; show elapsed progress in RefinementConversation
- Backend: PatchArtifact + API (state, patch-status) include feedback_id, refinement_conversation_id, started_at, elapsed_seconds
- Activity: in-progress cards show "View discussion", terminal cards "Discussion"; onOpenRefinement opens refinement and closes sheet
- PatchEntry and requestPatch/poll/applyState carry new fields
```

## Next Step

**PM acceptance:** Accept T-0097 and move to `tickets/status/done/` per `tickets/AGENTS.md` "PM Acceptance (Post-QA)".
