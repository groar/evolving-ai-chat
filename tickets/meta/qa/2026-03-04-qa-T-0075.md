# QA Checkpoint — T-0075: M10 Live Apply — Hot-Reload After Patch Acceptance

**Date**: 2026-03-04
**Ticket**: T-0075
**QA trigger**: Automatic post-implementation phase (Development Workflow Mode)
**Verdict**: ✅ PASS — no bugs filed

---

## Test Execution

### Automated tests
```
Command: npm run validate (apps/desktop)
Result:  All tests pass
- PatchNotification.test.tsx: 55 tests (incl. new reloading state tests)
- useRuntime.modelSelection.test.ts: 5 tests
- Other suites: feedbackPanel, activitySheet, settingsPanel, etc.
```

---

## AC-by-AC Validation

| # | Acceptance Criterion | Status | Evidence |
|---|---|---|---|
| 1 | After stub patch reaches `applied`, page reloads within ~1s | ⏳ Manual | Requires PATCH_AGENT_STUB=true; runtime + desktop app. See Manual Validation below. |
| 2 | During ~400ms pre-reload, PatchNotification shows "Applying your update — reloading…" + spinner | ✅ PASS | `PatchNotification.test.tsx`: `reloading shows spec copy`; `shows spinner for reloading state` |
| 3 | After reload, PatchNotification reappears with `applied` state | ⏳ Manual | Same as #1; `/state` returns patches; notification driven by backend |
| 4 | Clicking "Undo" before reload fires cancels reload and proceeds with rollback | ✅ PASS | `cancelReloadIfScheduled()` called at start of `rollbackPatch`; `PatchNotification` shows Undo during reloading |
| 5 | All 9 PatchNotification states render correctly (regression) | ✅ PASS | Tests cover all states including `reloading` |
| 6 | useRuntime unit tests pass; no TypeScript errors | ✅ PASS | `npm run validate` passes |

---

## UX/UI Design QA (Area: ui)

**Scope**: `reloading` display state in PatchNotification (transient ~400ms before `window.location.reload()`).

### UX_QA_CHECKLIST (excerpt)

| Category | Result | Notes |
|---|---|---|
| 1) Mental Model | PASS | "Applying your update — reloading…" answers "what is happening"; change is live soon |
| 2) Hierarchy | PASS | Spinner + message; Undo remains primary action during reload (user can cancel) |
| 3) IA/Navigation | N/A | Single notification banner, no nav change |
| 4) States | PASS | `reloading` is clear transient state; no new error state added |
| 5) Copy | PASS | "reloading" is non-technical; does not say "Done" or "Complete" (spec constraint) |
| 6) Affordances | PASS | Undo button visible during reload; sized appropriately |
| 7) Visual Accessibility | PASS | Reuses applied-state styling (green border/bg); spinner matches other in-progress states |
| 8) Responsive | PASS | Same layout as existing notification; no new layout |

**UX Verdict**: PASS — no follow-up tickets.

---

## Copy Regression Sweep

**New copy**: "Applying your update — reloading…"

- Term consistency: "Applying" matches existing "Applying change…" (applying state).
- Promise control: Does not imply "complete" or "done"; clearly in-progress.
- No typos or ambiguity.

---

## Manual Validation (Deferred)

Full end-to-end validation requires:

1. `cd apps/desktop/runtime && PATCH_AGENT_STUB=true uv run fastapi dev main.py`
2. `cd apps/desktop && npm run dev` (or Tauri dev)
3. Trigger improvement flow (Improvement → submit feedback)
4. Observe: "Applying your update — reloading…" for ~400ms → page reloads → notification reappears with "Undo?"

**Recommendation**: User or PM to run this once before release; automated tests cover component-level behavior.

---

## Verdict

- Automated: ✅ PASS
- UX checklist: ✅ PASS
- Copy sweep: ✅ PASS
- Manual E2E: ⏳ Deferred (documented steps)

Ticket ready for PM acceptance. No bugs filed.

---

## Suggested Commit Message

```
feat(T-0075): M10 live apply — hot-reload after patch acceptance

- Add reloading display state to PatchNotification
- Trigger window.location.reload() 400ms after applied
- Cancel reload when Undo clicked before reload
- Tests for reloading state and Undo during reload
```

---

## Next Step

**Recommended**: PM acceptance — move T-0075 to `tickets/status/done/` per `tickets/AGENTS.md` "PM Acceptance (Post-QA)".

**Optional**: Run manual E2E with PATCH_AGENT_STUB=true before next deploy.
