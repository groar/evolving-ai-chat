# QA Checkpoint — T-0078: Fix test_chat.py Chat Mock Injection

**Date**: 2026-03-05  
**Ticket**: T-0078  
**QA trigger**: Automatic post-implementation phase (Development Workflow Mode)  
**Verdict**: ✅ PASS — no bugs filed

---

## Test Execution

### Automated tests
```
Command: cd apps/desktop && uv run pytest runtime/test_chat.py -q
Result:  20 passed

Command: cd apps/desktop && uv run pytest runtime/test_proposals.py runtime/test_apply_rollback.py -q
Result:  24 passed
```

### Scope and isolation notes

- Tests now use `app.dependency_overrides[get_chat_router]` to inject fake chat and streaming handlers.
- This ensures no live network calls are made during `test_chat.py` runs; the OpenAI/Anthropic adapters are never invoked in these unit tests.
- The runtime app continues to use the same `ChatRouter` behavior in production (no user-facing behavior changes), with the new dependency hook only affecting tests that opt in via overrides.

---

## AC-by-AC Validation

| # | Acceptance Criterion | Status | Evidence |
|---|----------------------|--------|----------|
| 1 | `uv run pytest runtime/test_chat.py -v` exits 0 from `apps/desktop/` | ✅ PASS | `20 passed` from `uv run pytest runtime/test_chat.py -q` |
| 2 | All tests in `test_chat.py` that were previously failing with 502 now pass | ✅ PASS | No remaining 502 assertions; all chat endpoint tests green |
| 3 | No live network calls are made during the test run (mock fully isolates the adapter) | ✅ PASS | Chat and streaming tests rely solely on injected fakes via dependency overrides |
| 4 | Production code in `main.py` or the chat router is only changed if required to allow clean dependency injection; no behavior changes for end users | ✅ PASS | `get_chat_router` FastAPI dependency returns the existing `chat_router` instance; request handling semantics are unchanged without overrides |
| 5 | `uv run pytest runtime/test_proposals.py runtime/test_apply_rollback.py` is not broken by this change (no cross-test regressions) | ✅ PASS | `24 passed` from `uv run pytest runtime/test_proposals.py runtime/test_apply_rollback.py -q` |

---

## Notes

- This ticket removes the need to patch `runtime.main.chat_router.chat` directly in tests, which previously failed to take effect at request time when using `TestClient`.
- The new pattern (FastAPI dependency + `dependency_overrides`) should be the default for future adapter-level tests that need strict isolation from live providers.

---

## Verdict

All acceptance criteria are satisfied with deterministic pytest evidence and no regressions in related runtime tests. Ticket is ready for PM acceptance.

