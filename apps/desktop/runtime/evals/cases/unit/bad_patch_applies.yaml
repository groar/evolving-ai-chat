id: "bad-patch-applies"
description: "A malformed or inapplicable diff should fail the check"
check_type: "patch_applies_cleanly"
blocking: true
inputs:
  patch_file: "../fixtures/bad.diff"
  # Same minimal fixture (NonexistentFile.tsx not present → patch fails)
  target_dir: "apps/desktop/runtime/evals/cases/unit/fixture_minimal"
expect:
  result: "fail"
