{
  "version": "1",
  "allow_patterns": [
    "^apps/desktop/src/",
    "^apps/desktop/runtime/",
    "^tickets/",
    "^docs/",
    "^tests/"
  ],
  "notes": "Extended scope: UI (apps/desktop/src/), runtime (apps/desktop/runtime/), tickets, docs, tests. Do not add to allow_patterns without updating the scope guard system prompt and the QA checkpoint."
}
