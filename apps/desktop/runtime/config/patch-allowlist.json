{
  "version": "1",
  "allow_patterns": [
    "^.*"
  ],
  "notes": "Permissive: all paths allowed. Add restrictive patterns when ready; then update the scope guard system prompt and QA checkpoint."
}
